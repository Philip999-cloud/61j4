# ═══ 上架／筆電測試（請在執行 npm run pack:laptop 前讀）═══
# ‧ pack:laptop / pack 會先跑 npm run check:release-env：須有有效 GEMINI_API_KEY、VITE_FIREBASE_API_KEY（非占位字）。
# ‧ 建置當下會把 GEMINI_API_KEY、VITE_* 等寫入前端 bundle；換電腦或環境請改 .env 後重新打包。
# ‧ 純靜態「npm run serve:laptop」不會轉發 /api。若功能依賴 Serverless API：
#     終端 1：npx vercel dev（預設常為 :3000，與 vite preview 代理一致；若改埠請設 VERCEL_DEV_API_ORIGIN）
#     終端 2：npm run serve:laptop:preview（讀 ../asea-laptop-test，含 vite.config 的 /api 代理）
# ‧ 開發時 Vite 佔 :3000、後端常用 npm run dev:api（:3001）時，請勿與上述 preview 埠混淆；preview 預設轉發 3000。
# ‧ 建置時若出現 @rdkit「Module fs/crypto externalized」警告，屬既有提示，可忽略。
#
# Gemini：僅放本機 .env，勿 commit；部署時用 Vercel／主機環境變數
GEMINI_API_KEY=your_gemini_api_key
# 客服對話等前端功能（會被打包進前端，務必加 API 金鑰限制，見 Google Cloud Console）
VITE_GEMINI_API_KEY=
# 選填：預設 gemini-3.1-pro-preview；若瀏覽器常出現 net::ERR_CONNECTION_CLOSED／Failed to fetch，可改設 Google AI Studio 目前可用的其他 Pro 模型 ID
# VITE_GEMINI_PRO_MODEL=gemini-2.5-pro-preview-05-06

# Firebase Config（從 Firebase Console 取得，見 FIREBASE_SETUP.md）
# 部署到 Vercel 時：務必在 Firebase → Authentication → Settings → Authorized domains
# 新增「網站網域」例如 asea-smart-education.vercel.app（勿把 authDomain 改成這個）
# 本機若用 http://127.0.0.1:... 或區網 IP，須在 Authorized domains 另外新增「127.0.0.1」或該 IP（與 localhost 分開）
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# 金流 API（Phase 3，見 FIREBASE_SETUP.md 或後端文件）
VITE_PAYMENT_API_URL=
VITE_SUBSCRIPTION_API_URL=

# 客服信件（選填，用於即時發送客服表單至管理員信箱）
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
VITE_ADMIN_EMAIL=

# 本地 Serverless API（圖片生成、幾何萃取 extract-geometry 等）：由 Vercel 本機模擬
# ─ npm run dev（Vite 佔 :3000）時：瀏覽器打 /api/* 會由 Vite 轉發到下方「後端埠」。
#   未設定 VERCEL_DEV_API_ORIGIN 時，vite.config 預設轉發到 http://127.0.0.1:3001（避免與 Vite 3000 衝突）。
#   請另開終端執行：npm run dev:api（等同 vercel dev --listen 3001）
# ─ npm run preview（常為 :4173）時：未設定則預設轉發到 http://127.0.0.1:3000。
#   此時可執行：npx vercel dev（預設常為 3000），或 vercel dev --listen 與 VERCEL_DEV_API_ORIGIN 一致。
# 若你自訂 vercel dev 埠，請設 VERCEL_DEV_API_ORIGIN 與該埠一致（勿與 Vite dev 的 3000 相同）。
# 修改 .env 後：前端請重啟 npm run dev；僅改後端 api 時通常重啟 vercel dev 即可。
# VERCEL_DEV_API_ORIGIN=
# VITE_IMAGE_GENERATION_API_URL=
# （上兩行可維持註解不填：圖片 API 可走相對路徑 /api/generate-image。若前端需直連，請設與 vercel dev 實際埠一致，例如 http://127.0.0.1:3001/api/generate-image）

# Python Matplotlib 函數圖沙箱（npm run dev 時 Vite 將 POST /api/python-plot 轉發至此）
# 啟動：cd python_plot_sandbox && pip install -r requirements.txt && uvicorn app:app --host 127.0.0.1 --port 8765
VITE_PYTHON_PLOT_API_ORIGIN=http://127.0.0.1:8765
# 選填：生產環境直連完整 URL 時設定（未設則用相對路徑 /api/python-plot）
# VITE_PYTHON_PLOT_API_URL=

# ASEA DSL 渲染（SymPy 數學／RDKit 化學／生物母版）；未設時與沙箱同源，dev 透過 Vite 代理 /api/v1 → VITE_PYTHON_PLOT_API_ORIGIN
# VITE_ASEA_RENDER_API_URL=http://127.0.0.1:8765

# python_plot_sandbox 伺服器端（非 Vite；於執行 uvicorn 的環境設定）：
# ASEA_RENDER_TIMEOUT_SEC=5
# ASEA_RENDER_MAX_PAYLOAD_BYTES=49152
# ASEA_RENDER_CACHE_MAX=512
# ASEA_RENDER_CACHE_TTL_SEC=3600
# ASEA_MATH_MAX_EQ_LEN=384
# ASEA_MATH_MAX_DOMAIN_SPAN=800
# ASEA_CHEM_MAX_SMILES_LEN=4096
